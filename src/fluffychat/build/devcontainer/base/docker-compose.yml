services:
  fluffychat:
    image: ghcr.io/krille-chan/fluffychat:latest
    container_name: fluffychat
    restart: unless-stopped
    networks:
      - fluffychat-workspace-network
networks:
  fluffychat-workspace-network:
    external: true
